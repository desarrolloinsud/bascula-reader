# Configuración del conector de báscula
# =====================================
# Este archivo define cómo se conecta el ejecutable a la báscula
# y cómo expone la API HTTP local.

# Puerto HTTP local donde expone la API
# Normalmente 7070. La web le pegará a http://127.0.0.1:SERVER_PORT
SERVER_PORT=7070

# Puerto serie de la báscula
# Windows: COM1, COM2, COM3, etc.
# Linux:   /dev/ttyUSB0, /dev/ttyS0, etc.
# macOS:   /dev/tty.usbserial-XXXX, /dev/tty.usbmodem-XXXX, etc.
SERIAL_PORT=COM1

# Baudios de la báscula (velocidad)
# Valor típico: 9600
BAUD_RATE=9600

# Dominio del frontend que puede acceder (para CORS)
# Para desarrollo local / demo:
#   ALLOWED_ORIGIN=*
# Para producción, idealmente tu dominio real, ej:
#   ALLOWED_ORIGIN=https://mi-frontend.com
ALLOWED_ORIGIN=https://cfc.fresa.com.ar

# Modo simulación:
#   true  -> no usa puerto serie, genera pesos ficticios (para pruebas)
#   false -> usa la báscula real
MOCK_SCALE=false

# Identificador lógico de la báscula (sucursal, caja, etc.)
# Este valor viaja en las respuestas JSON como "scale_id"
# y sirve para saber qué báscula emitió cada lectura.
SCALE_ID=bascula-1